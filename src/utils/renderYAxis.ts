/**
 * 对 y轴 进行处理
 * @param {object} options x 轴的配置项
 */

const renderYAxis = (options: any) => {
    // 暂时只考虑单轴的情况
    const y_options = {
        type: 'value',
        show: false,
    }

    return y_options;
}

export default renderYAxis;